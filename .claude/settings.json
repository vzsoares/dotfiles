{
  "permissions": {
    "additionalDirectories": [
      "/home/zizmackrok/code"
    ]
  },
  "model": "sonnet",
  "skillOverrides": {
    "auto-mode-setup": "off"
  },
  "hooks": {
    "Notification": [
      {
        "matcher": "^(?!idle_prompt$)",
        "hooks": [
          {
            "type": "command",
            "command": "p=$(timeout 1 jq -r \".cwd // empty\" 2>/dev/null); p=${p:-$PWD}; n=$(git -C \"$p\" rev-parse --show-toplevel 2>/dev/null || echo \"$p\"); notify-send -t 0 \"Claude Code — ${n##*/}\" \"Needs your attention\"; zen-bell window-attention",
            "async": true
          }
        ]
      }
    ],
    "Stop": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "p=$(timeout 1 jq -r \".cwd // empty\" 2>/dev/null); p=${p:-$PWD}; n=$(git -C \"$p\" rev-parse --show-toplevel 2>/dev/null || echo \"$p\"); zen-bell complete -t 0 -n \"Claude Code — ${n##*/}\" -b \"Turn finished\"",
            "async": true
          }
        ]
      }
    ],
    "SessionStart": []
  },
  "statusLine": {
    "type": "command",
    "command": "$HOME/.claude/skills/clawd-status/scripts/statusline.sh",
    "refreshInterval": 1
  },
  "enabledPlugins": {},
  "autoCompactWindow": 800000,
  "tui": "default",
  "skipDangerousModePermissionPrompt": false,
  "skipWorkflowUsageWarning": true,
  "editorMode": "vim",
  "agentPushNotifEnabled": true,
  "mcpServers": {
    "playwright": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "-y",
        "@playwright/mcp@latest",
        "--output-dir",
        "/tmp/playwright-mcp"
      ]
    },
    "docs-mcp-server": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "-y",
        "@arabold/docs-mcp-server@latest"
      ],
      "env": {
        "OPENAI_API_KEY": "ollama",
        "OPENAI_API_BASE": "http://localhost:11434/v1",
        "DOCS_MCP_EMBEDDING_MODEL": "openai:nomic-embed-text"
      }
    }
  }
}
